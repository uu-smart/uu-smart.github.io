import { defineCollection } from 'vuepress-theme-plume'

export default defineCollection({
  type: 'post',
  dir: 'blog',
  title: 'Blog',
  link: '/blog/',
  // linkPrefix: '/article/',
  // postList: true,
  // tags: true,
  // archives: true,
  // categories: true,
})
